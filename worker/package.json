{
  "name": "juranyi-backend",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init:remote": "wrangler d1 execute juranyi-db --remote --file=./schema.sql",
    "db:init:local": "wrangler d1 execute juranyi-db --local --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
